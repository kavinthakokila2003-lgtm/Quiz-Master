{
  "name": "quiz-master-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Quiz tournament on Cloudflare Workers and D1",
  "scripts": { "deploy": "wrangler deploy" },
  "devDependencies": { "wrangler": "^4.0.0" }
}
